import React from "react";

export default function CheckoutPage({ children }) {
  // make api call there (testing knowledge)

  return (
    <React.Fragment>
      <h1>This is the Checkout Page</h1>
    </React.Fragment>
  );
}
