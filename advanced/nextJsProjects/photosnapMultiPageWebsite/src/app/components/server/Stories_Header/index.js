import React from "react";
import styles from "./Stories_Header.module.css";

export default function StoriesHeader({ children }) {
  return (
    <header>
      <div>
        {/* img */}
        {/* text content */}
      </div>
    </header>
  );
}
